---
id: close_equipes_documentos
title: close_equipes_documentos
sidebar_position: 34
description: Encerra associação de documentos com a equipe.
---

# `close_equipes_documentos.pl`

Encerra associação de documentos com a equipe.

## Overview

This page was generated automatically from the Perl source file.

## Perl Modules

The script uses the following modules:

- `DBI`
- `Getopt::Std`
- `POSIX`

## Database

- **Database interface:** MySQL via Perl DBI

## Command-Line Options

| Option | Argument |
|---|---|
| `-w` | No |
| `-h` | No |
| `-u` | Required |
| `-p` | Required |
| `-f` | No |

### Example

```bash
perl close_equipes_documentos.pl -h
```

## Source Code

```perl
#!/usr/bin/perl
use strict;
use warnings;
use DBI;
use Getopt::Std;
use POSIX qw(strftime);

# -----------------------------------------------------------------------------
# Encerra associação de documentos com a equipe.
# -----------------------------------------------------------------------------
$|++;
my $crlf = "\n";
my $aplicativo = $0;


# definição para acesso do banco (valores default)
my $host         = "localhost";
my $database     = "DAELT";
my $usuario      = "";
my $senha        = "";
my $final_commit = 0;


# Recuperação dos parâmetros de entrada (via shell)
my %opcoes=();
getopts("whu:p:f", \%opcoes);

$crlf         = "\r\n"     if defined $opcoes{w};
$usuario      = $opcoes{u} if defined $opcoes{u};
$senha        = $opcoes{p} if defined $opcoes{p};
$final_commit = 1          if defined $opcoes{f};

if (defined $opcoes{h})
{
	print STDERR "$aplicativo [-w] [-u <usuario>] [-p <senha>] [-f]$crlf";
	exit 0;
}


# apresentação do programa
print STDERR "$aplicativo\::Encerra associação de documentos " .
             "com a equipe.$crlf";


# conexão com banco de dados
my ($dbHandle, $dbStatement);

$dbHandle = DBI->connect("DBI:mysql:database=$database;host=$host",
                         $usuario, $senha,
                         {PrintError => 0, AutoCommit => 0})
	or die "$aplicativo\::Falha de conexão ao banco de dados: $DBI::errstr";

$dbStatement = $dbHandle->prepare(
	"UPDATE TCC.EQUIPES_DOCUMENTOS SET DATA = ? " .
	"WHERE ID_EQUIPE = ? AND ID_DOCUMENTO = ? AND DATA IS NULL;");


# definição de data de início para TODAS as equipes
my $equipe_datainicio = strftime "%Y-%02m-%02d", localtime;


# obtém nome da equipe
print STDERR "Cód equipe <spc> núm docs <spc> " .
             "id doc <spc> id doc ... (CTRL+D aborta):";
while (<STDIN>)
{
	my @informacao = split(' ');

	my $informacaoSize = scalar(@informacao);
	(print STDERR "$aplicativo\::Número de parâmetros incorretos$crlf" and
	 $dbHandle->disconnect() and die)
		if ($informacaoSize < 2);

	my $equipe_id = $informacao[0];

	(print STDERR "$aplicativo\::Número de parâmetros incorretos$crlf" and
	 $dbHandle->disconnect() and die)
		if ($informacaoSize != $informacao[1] + 2);

	for (my $i=0; $i<$informacao[1]; $i++)
	{
		$dbStatement->bind_param(1, $equipe_datainicio);
		$dbStatement->bind_param(2, $equipe_id);
		$dbStatement->bind_param(3, $informacao[$i + 2]);

		$dbStatement->execute()
			or (print "$aplicativo\::Falha de fechamento: " .
			          "$DBI::errstr$crlf" and
			    $dbHandle->disconnect() and die);
	}

	$dbHandle->commit() if $final_commit==0;

	print STDERR "Cód equipe <spc> núm docs <spc> " .
	             "id doc <spc> id doc ... (CTRL+D aborta):";
}


# desconexão com banco de dados
$dbHandle->commit() if $final_commit==1;
$dbHandle->disconnect();
exit 0;
```
