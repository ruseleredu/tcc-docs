---
id: send_atraso_avaltcc1
title: send_atraso_avaltcc1
sidebar_position: 79
description: Envia email de aviso de atraso da defesa parcial de TCC.
---

# `send_atraso_avaltcc1.pl`

Envia email de aviso de atraso da defesa parcial de TCC.

## Overview

This page was generated automatically from the Perl source file.

## Perl Modules

The script uses the following modules:

- `DBI`
- `Getopt::Std`
- `POSIX`
- `Mail::Sendmail`

## Database

- **Database interface:** MySQL via Perl DBI

## Command-Line Options

| Option | Argument |
|---|---|
| `-w` | No |
| `-h` | No |
| `-u` | Required |
| `-p` | Required |
| `-t` | No |

### Example

```bash
perl send_atraso_avaltcc1.pl -h
```

## Source Code

```perl
#!/usr/bin/perl
use strict;
use warnings;
use DBI;
use Getopt::Std;
use POSIX qw(strftime);
use Mail::Sendmail qw(sendmail %mailcfg);

# -----------------------------------------------------------------------------
# Envia email de aviso de atraso da defesa parcial de TCC.
# Envio para orientadores da equipe. Não faz checagem de documento já entregue.
# -----------------------------------------------------------------------------
$|++;
my $crlf = "\n";
my $aplicativo = $0;


# definição de servidor SMTP para envio de mensagens
# $mailcfg{smtp} = [qw(spamfirewall2.utfpr.edu.br)];

# definição para acesso do banco (valores default)
my $host         = "localhost";
my $database     = "DAELT";
my $usuario      = "";
my $senha        = "";

my $tela         = 0;


# Recuperação dos parâmetros de entrada (via shell)
my %opcoes=();
getopts("whu:p:t", \%opcoes);

$crlf         = "\r\n"     if defined $opcoes{w};
$usuario      = $opcoes{u} if defined $opcoes{u};
$senha        = $opcoes{p} if defined $opcoes{p};

$tela         = 1          if defined $opcoes{t};

if (defined $opcoes{h})
{
	print STDERR "$aplicativo [-w] [-u <usuario>] [-p <senha>] [-t]$crlf";
	exit 0;
}


# apresentação do programa
print STDERR "$aplicativo\::Aviso de atraso da defesa parcial de TCC.$crlf";


# conexão com banco de dados
my ($dbHandle, $dbStatement1, $dbStatement2);
my (@dbLinha);

$dbHandle = DBI->connect("DBI:mysql:database=$database;host=$host",
                         $usuario, $senha,
                         {PrintError => 0, AutoCommit => 0})
	or die "$aplicativo\::Falha de conexão ao banco de dados: $DBI::errstr";

$dbStatement1 = $dbHandle->prepare(
	"SELECT A.ID, A.TITULO, D.NOME, D.EMAIL, D.EMAIL2 " .
	"FROM TCC.EQUIPES A, TCC.EQUIPES_DISCIPLINAS B, " .
	     "DAELT.DOMINIO_DISCIPLINAS C, DAELT.PROFESSORES D " .
	"WHERE A.ID = ? AND A.ID = B.ID_EQUIPE AND B.ID_DISCIPLINA = C.ID AND " .
	      "B.DATA_FIM IS NULL C.ID_RESPONSAVEL = D.ID;");
$dbStatement2 = $dbHandle->prepare(
    "SELECT B.NOME, B.EMAIL, B.EMAIL2 " .
    "FROM TCC.EQUIPES_ORIENTADORES A, DAELT.PROFESSORES B " .
    "WHERE A.ID_EQUIPE = ? AND ORIENTACAO = '1' AND A.DATA_FIM IS NULL AND " .
          "A.ID_ORIENTADOR = B.ID;");

# definição de data de início para TODAS as equipes
my $equipe_datainicio = strftime "%Y-%02m-%02d", localtime;


# obtém nome do aluno
print STDERR "Cód equipe (CTRL+D aborta):";
while (<STDIN>)
{
	(print STDERR "$aplicativo\::Entrada incorreta$crlf" and
	 $dbHandle->disconnect() and die)
		if (!/\s*(\S+)\s*/);

	my $equipe_id = $1;

	# carrega nomes/emails dos orientadores
	$dbStatement2->bind_param(1, $equipe_id);
	$dbStatement2->execute();

	my $nomes_orientadores  = "";
	my $addrs_orientadores = "";
	my $existe_nomes = 0;
	while ( @dbLinha = $dbStatement2->fetchrow_array )
	{
		if ( $existe_nomes==0 )
		{
			$nomes_orientadores = $dbLinha[0] if $existe_nomes == 0;
			$addrs_orientadores = $dbLinha[1] if $existe_nomes == 0;

			$existe_nomes = 1;
		}
		else
		{
			$nomes_orientadores = $nomes_orientadores . ", " . $dbLinha[0];
			$addrs_orientadores = $addrs_orientadores . ", " . $dbLinha[1];
		}
	}

	(print STDERR "$aplicativo\::Email de destino não existe$crlf" and
	 $dbHandle->disconnect() and die)
		if ( !length($addrs_orientadores) );

	# busca dados da equipe para email
	$dbStatement1->bind_param(1, $equipe_id);
	$dbStatement1->execute();

	while ( @dbLinha = $dbStatement1->fetchrow_array )
	{
		my $email_body = <<EMAIL_BODY;
Prezado(s) orientador(es) $nomes_orientadores,

Solicito envio da avaliação da defesa parcial de TCC (TCC1)

$equipe_id - $dbLinha[1]

o mais breve possível, para que eu possa lançar a nota no sistema acadêmico, e avisar os alunos da aprovação ou reprovação.

Tal urgência se faz necessária para que os prazos regimentais sejam seguidos, evitando dissabores para todos nós.

Fico a disposição para qualquer dúvida.

Att.,
$dbLinha[2]
Responsável pelo TCC/DAELT.
EMAIL_BODY

		# prepara email do responsável pelo TCC
		# pode ser usado em 'reply-to'
		my $email_source;
		if (defined $dbLinha[3] and defined $dbLinha[4])
		{
			$email_source = $dbLinha[3] . ", " . $dbLinha[4];
		}
		elsif (defined $dbLinha[3])
		{
			$email_source = $dbLinha[3];
		}
		elsif (defined $dbLinha[4])
		{
			$email_source = $dbLinha[4];
		}

		my %email = (
		    to             => $addrs_orientadores,
		    from           => 'mrosa@nupet.daelt.ct.utfpr.edu.br',
		    'reply-to'     => 'tcc.daelt.utfpr@gmail.com',
		    subject        => "Resultado defesa parcial de TCC (TCC1)",
		    message        => $email_body,
		    'content-type' => 'text/plain; charset="utf-8"',
		);

		if ($tela == 0)
		{
			sendmail(%email)
				or (print STDERR "$aplicativo\::Falha de envio: " .
					             "$Mail::Sendmail::error$crlf" and
					    $dbHandle->disconnect() and die);

			print STDERR "Sent to $addrs_orientadores " .
			             "Result = $Mail::Sendmail::log$crlf";
		}
		else
		{
			print "----------------------------------------------> email$crlf";
			print %email;
			print $crlf;
		}
	}

	print STDERR "Cód equipe (CTRL+D aborta):";
}


# desconexão com banco de dados
$dbHandle->disconnect();
exit 0;
```
